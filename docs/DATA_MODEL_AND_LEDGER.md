# Data Model and Ledger

Truth chain: `clock event -> JSONL append -> event hash -> SQLite mirror -> owner export`.

Event types: `clock_in`, `clock_out`, `break_start`, `break_end`, `admin_adjustment`.
