# Continuation Handoff — v0.9.0

Continue from `docs/SYSTEM_STATE_CURRENT.md` and
`pipeline/arc_employee_management_kiosk_timeclock.v0.9.0.json`.

The next arc should design named manager users and capability-scoped tokens without
weakening the owner-token recovery root. Preserve separate employee and guest ledgers,
soft-delete history, human-readable exports, and server-authoritative active sessions.
